'use client';

// This component is no longer used separately.
// Pre-classifier panel is now rendered inside StepDetail.tsx.
// Keeping file to avoid import errors during transition.

export default function PreClassDetail() {
  return null;
}
